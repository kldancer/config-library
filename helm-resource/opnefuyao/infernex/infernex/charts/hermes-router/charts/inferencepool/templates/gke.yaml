{{- if eq (lower .Values.provider.name) "gke" }}
---
kind: HealthCheckPolicy
apiVersion: networking.gke.io/v1
metadata:
  name: {{ .Values.global.name | default .Release.Name }}
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    {{- include "gateway-api-inference-extension.labels" . | nindent 4 }}
spec:
  targetRef:
    group: "{{ (split "/" .Values.inferencePool.apiVersion)._0 }}"
    kind: InferencePool
    name: {{ .Values.global.name | default .Release.Name }}
  default:
    # Set a more aggressive health check than the default 5s for faster switch
    # over during EPP rollout.
    timeoutSec: 2
    checkIntervalSec: 2
    config:
      type: HTTP
      httpHealthCheck:
          requestPath: /health
          port:  {{ .Values.inferencePool.targetPortNumber }}
---
apiVersion: networking.gke.io/v1
kind: GCPBackendPolicy
metadata:
  name: {{ .Values.global.name | default .Release.Name }}
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    {{- include "gateway-api-inference-extension.labels" . | nindent 4 }}
spec:
  targetRef:
    group: "{{ (split "/" .Values.inferencePool.apiVersion)._0 }}"
    kind: InferencePool
    name: {{ .Values.global.name | default .Release.Name }}
  default:
    timeoutSec: 300    # 5-minute timeout (adjust as needed)
    logging:
      enabled: true    # log all requests by default
---
{{- if or .Values.inferenceExtension.monitoring.gke.enabled (and .Values.inferenceExtension.monitoring.prometheus.enabled .Values.inferenceExtension.monitoring.prometheus.auth.enabled) }}
{{- $releaseName := .Values.global.name | default .Release.Name -}}
{{- $releaseNamespace := .Values.global.namespace | default .Release.Namespace -}}
{{- $metricsReadSA := printf "%s-metrics-reader-sa" $releaseName -}}
{{- $metricsReadSecretName := printf "%s-metrics-reader-secret" $releaseName -}}
{{- $metricsReadRoleName := printf "%s-%s-metrics-reader" $releaseNamespace $releaseName -}}
{{- $metricsReadRoleBindingName := printf "%s-%s-metrics-reader-role-binding" $releaseNamespace $releaseName -}}
{{- $secretReadRoleName := printf "%s-metrics-reader-secret-read" $releaseName -}}
{{- $gmpNamespace := "gmp-system" -}}
{{- $isAutopilot := false -}}
{{- with .Values.provider.gke }}
  {{- $isAutopilot = .autopilot | default false -}}
{{- end }}
{{- if $isAutopilot -}}
{{-   $gmpNamespace = "gke-gmp-system" -}}
{{- end -}}
{{- $gmpCollectorRoleBindingName := printf "%s:collector:%s-%s-metrics-reader-secret-read" $gmpNamespace $releaseNamespace $releaseName -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $metricsReadSA }}
  namespace: {{ $releaseNamespace }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $metricsReadSecretName }}
  namespace: {{ $releaseNamespace }}
  labels:
    {{- include "gateway-api-inference-extension.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/service-account.name: {{ $metricsReadSA }}
type: kubernetes.io/service-account-token
---
apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: {{ $releaseName }}
  namespace: {{ $releaseNamespace }}
  labels:
    {{- include "gateway-api-inference-extension.labels" . | nindent 4 }}
spec:
  endpoints:
  - port: metrics
    scheme: http
    interval: {{ .Values.inferenceExtension.monitoring.interval }}
    path: /metrics
    authorization:
      type: Bearer
      credentials:
        secret:
          name: {{ $metricsReadSecretName }}
          key: token
  selector:
    matchLabels:
      {{- include "gateway-api-inference-extension.selectorLabels" . | nindent 8 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ $metricsReadRoleName }}
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ $metricsReadRoleBindingName }}
subjects:
- kind: ServiceAccount
  name: {{ $metricsReadSA }}
  namespace: {{ $releaseNamespace }}
roleRef:
  kind: ClusterRole
  name: {{ $metricsReadRoleName }}
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ $secretReadRoleName }}
rules:
- resources:
  - secrets
  apiGroups: [""]
  verbs: ["get", "list", "watch"]
  resourceNames: [{{ $metricsReadSecretName | quote }}]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ $gmpCollectorRoleBindingName }}
  namespace: {{ $releaseNamespace }}
roleRef:
  name: {{ $secretReadRoleName }}
  kind: Role
  apiGroup: rbac.authorization.k8s.io
subjects:
- name: collector
  namespace: {{ $gmpNamespace }}
  kind: ServiceAccount
{{- end }}
{{- end }}
